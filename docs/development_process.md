# Журнал разработки

Этот файл содержит хронологию выполненных работ по проекту.

---

## 19 мая 2026 г.

* **Планирование спринта v0.2.4:** Составлен и утвержден план работ на следующий спринт. План сфокусирован на погашении технического долга, улучшении архитектуры и исправлении багов. Итоговый план сохранен в файле `docs/planing/plan_v0_2_4.md`.

## 15 мая 2026 г

* **Исправление бага: Кнопка добавления связей пропадает:** Исправлена ошибка в `ConnectionsManager.tsx`, из-за которой кнопка "Добавить новую связь" пропадала при наличии существующих связей. Проведен рефакторинг компонента для унификации логики отображения заголовка и контента, чтобы кнопка всегда была видна. Это включало изменение `headerContent` для условного отображения счетчика связей и консолидацию двух путей рендеринга в один, более надежный.

## 14 мая 2026 г

* **Графическое отображение связей:** Реализован компонент `ConnectionGraph.tsx` для интерактивного графического отображения связей сущности. Внедрен в `ConnectionsManager.tsx` как альтернативный вид. Отключено перетаскивание узлов и анимация ребер для улучшения пользовательского опыта.
* **Добавление зависимостей:** Добавлена зависимость "reactflow": "^11.11.4" для графического отображения графа связей.
* **Реализация фильтруемого списка связей:**
  * **Внедрение строгой типизации и новой структуры для связей:**
    * Реализована строгая типизация объектов связей (`DetailedConnection`, `ConnectedEntity`, `WorldObjectInfoForConnection`) на всех уровнях приложения, включая корректное отображение имен связанных сущностей (объектов мира) с указанием шаблона.
    * Обновлены сервисы и DAO для формирования объектов `DetailedConnection`, включающих `connectionType` (направление) и полную информацию о связанной сущности.
    * Расширен IPC-обработчик `get-item-details` для предоставления фронтенду контекста (`all_entities_id`).
    * Адаптированы фронтенд-компоненты (`ConnectionsManager.tsx`, `ConnectionListItem.tsx`) для корректной работы с новой структурой.
  * **Динамическая фильтрация по шаблонам:** Список шаблонов для фильтрации теперь динамически формируется на основе шаблонов, присутствующих в текущих связях, что делает фильтр контекстно-зависимым и эффективным.
  * **Управление состоянием фильтров:** Реализовано управление состоянием для фильтров по типам сущностей (`Повествование`, `Объекты мира`) и по шаблонам (`templateIds`). Внедрена связанная логика взаимодействия чекбоксов (например, выбор "Объекты мира" включает все шаблоны, снятие последнего шаблона отключает "Объекты мира").
  * **Логика фильтрации:** Применена мемоизированная логика (`useMemo`) для эффективной фильтрации списка связей на основе активного состояния фильтров.
  * **Интеграция UI:** Элементы управления фильтрами полностью интегрированы с состоянием, а список связей динамически обновляется, отображая отфильтрованные результаты и актуальный счетчик.

* **Исправление ошибки "Cannot find name 'describe'" в тестах:** Добавлены `"jest"` и `"node"` в массив `"types"` в `compilerOptions` в файле `tsconfig.json`. Это решает проблему, когда TypeScript не мог найти глобальные определения типов для Jest, что приводило к ошибкам линтинга в тестовых файлах.

## 13 мая 2026 г

* **Визуальный рефакторинг компонента связей:** Проведен комплексный визуальный рефакторинг секции отображения связей в соответствии с обновленными требованиями дизайна.
  * **`ConnectionsManager.tsx`:** Компонент был переработан для использования сворачиваемой панели `Collapse`, которая отображается только при наличии связей. Фильтры были заменены на два основных чекбокса ("Повествование", "Объекты мира") и кнопку с выпадающим списком для под-фильтров.
  * **`ConnectionListItem.tsx`:** Полностью переработана верстка элемента списка. Вместо `Tag` и `Space` теперь используются `div`-блоки с CSS-классами для создания строгой табличной структуры.
  * **`App.css`:** Добавлены новые стили для поддержки табличной верстки в списке связей, обеспечивающие выравнивание и фиксированную ширину колонок.

## 08 мая 2026 г

* **Рефакторинг `ImportTemplatesModal.tsx`:** Перемещение элементов управления импортом объектов мира и связей в верхнюю панель. Добавлено информационное сообщение о наличии дубликатов шаблонов. Изменено поведение кнопки "Выбрать все" для игнорирования дубликатов. Исправление типа `ElectronAPI` в `src/main/preload.ts` для метода `checkTemplateNames`.

## 07 мая 2026 г

* **Summary of change:** Реализована визуальная индикация для дубликатов шаблонов в модальном окне импорта файлов, чтобы информировать пользователя о существующих шаблонах.

## 06 мая 2026 г

* **Summary of change:** Реализована принудительная синхронизация данных базы SQLite с диском перед архивацией проекта и при его закрытии. Это решает проблему потери данных при использовании WAL-режима, когда изменения могли не успеть записаться в основной файл базы данных до операции архивации. Для этого в `ProjectService` был добавлен метод `flushDatabase`, который выполняет `PRAGMA wal_checkpoint(RESTART)`, и этот метод был интегрирован в `ArchiveService` перед созданием архива, а также в метод `ProjectService.close()`.
* **Синхронизация "Дополнительных файлов"**: Реализован механизм периодического опроса (polling) для панели "Дополнительные файлы"

## 5 мая 2026 г

* **Улучшение обработки ошибок при создании проекта:** Проведен рефакторинг механизма обработки ошибок при создании нового проекта. Вместо пробрасывания исключений из `main` в `renderer` процесс, теперь `main` процесс самостоятельно перехватывает все ожидаемые (проект уже существует) и неожиданные (нет прав на запись) ошибки, показывает пользователю локализованное уведомление через `MainNotificationService` и возвращает в `renderer` только булево значение успеха/неудачи. Это устранило появление технических сообщений об ошибках IPC и сделало архитектуру более надежной.
* **Перенос экспорта из меню "Файл" в "Данные":** Реализовано перемещение пункта меню "Экспортировать объекты мира..." из подменю "Файл" в подменю "Данные" для улучшения логической структуры меню. Внесены изменения в файл `src/main/menu.ts`.
* **Переработка дизайна поля выбора папки в настройках:** Переработан дизайн компонента выбора пути в настройках проекта. Для большей компактности и лучшей визуальной интеграции, кнопка выбора файла была помещена внутрь поля ввода (в качестве `suffix`) и стилизована как иконка без фона и рамки.
* **Summary of change:** Переработана система уведомлений для операций импорта, экспорта и архивации. Вместо множества промежуточных уведомлений теперь отображается одно итоговое сообщение об успехе, ошибке или предупреждении, суммирующее результаты операции.

## 30 Апреля 2026 г

* **Обновление документации и руководства пользователя:** Актуализирована общая документация по проекту, а также обновлено руководство пользователя (`USER_GUIDE.md`) в соответствии с последними изменениями функционала.
* **Регрессионное тестирование:** Проведено комплексное регрессионное тестирование приложения, включающее проверку по тестовому чеклисту (`testing_checklist.md`) и использование существующих автоматизированных тестов, для подтверждения стабильности и корректной работы после внесенных изменений.

## 29 Апреля 2026 г

* **Summary of change:** Реализован механизм импорта объектов мира из внешних файлов в текущий проект. Включает десериализацию данных, создание соответствующих шаблонов и объектов в базе данных, а также восстановление связей между импортируемыми объектами. Добавлен пользовательский интерфейс для выбора импортируемых элементов и обработки возможных конфликтов.

## 26 апреля 2026 г

* **Улучшенный экспорт объектов мира:** Значительно доработан функционал экспорта объектов мира. Экспортный файл теперь содержит не только сами объекты и их шаблоны, но и связи между экспортируемыми объектами мира. Для обеспечения переносимости и независимости от внутренней структуры БД, в экспортном файле используются локальные ID. Реализована фильтрация связей, чтобы включать только те, что существуют между объектами мира, игнорируя связи с элементами повествования. Исправлены ошибки TypeScript и линтинга.

## 23 апреля 2026 г

* **Экспорт объектов мира:** Реализована функция экспорта всех объектов мира проекта в единый JSON-файл. Функция доступна через меню "Файл" -> "Экспортировать объекты мира...". Экспортируемый файл включает в себя данные всех объектов мира и определения связанных с ними шаблонов. Для предотвращения конфликтов при импорте, имена шаблонов в файле экспорта дополняются префиксом с названием проекта. Создан новый `ImportExportService` для инкапсуляции логики. Реализована вся необходимая обвязка в `main` и `renderer` процессах для работы нового пункта меню.

## 21 Апреля 2026 г

* **Summary of change:** Реализована новая функция архивирования проектов в ZIP-файл. Архив содержит основные компоненты проекта: базу данных SQLite, файл-маркер проекта (.wwb), а также директории narrative и world. Функция доступна через пункт меню и предоставляет обратную связь пользователю.

## 14 апреля 2026 г

* **Summary of change:** Реализован механизм именования файлов для **новых** объектов повествования на основе их уникального `ID` (`{id}.md`) вместо `slugify(name).md`. Это изменение устраняет проблему рассинхронизации имени файла и названия объекта при переименовании, а также предотвращает возможные коллизии имен. Для обеспечения атомарности операции, метод `NarrativeDao.createNarrativeItem` был реорганизован: теперь он принимает callback-функцию `getFilePath`, что позволяет в рамках одной транзакции создать запись, получить ее `ID` и обновить эту же запись путем к файлу, построенным на основе этого `ID`. Существующие объекты повествования не были затронуты и сохранили свои старые имена файлов.

## 06 Апреля 2026 г

* **Summary of change:** Внедрен механизм автоматической обрезки начальных и конечных пробелов (`trim()`) и очистки (санитайз) всех текстовых полей ввода пользовательских данных. Санитайз-обработка удаляет все символы, не являющиеся буквами (латиница, кириллица, включая 'ё'), цифрами или стандартными знаками препинания (.,!?'"-\s). Для реализации создан общий модуль `src/common/utils.ts` с функцией `cleanNameInput`, которая интегрирована во все соответствующие компоненты UI (модальные окна создания/редактирования объектов, шаблонов, связей и основной редактор контента).

## 2 апреля 2026 г

* **Summary of change:** Реализован механизм очистки области ContentDisplay при удалении выбранного объекта (как Narrative, так и World Object). В главном процессе (main.ts) были добавлены новые IPC-события 'item-deleted' после удаления narrative- и world-объектов. В процессе рендеринга (App.tsx) добавлен обработчик для этого события, который сбрасывает текущий выбор, если удаленный объект был выбран.
* **Summary of change:** Добавлена валидация при создании объектов повествования, запрещающая создание элементов с пустым именем.
* **Summary of change:** Реализовано предупреждение пользователя о невозможности архивирования шаблона, если он используется одним или несколькими объектами мира. Изменение ошибки (throw Error) на отправку уведомления через NotificationService.

## 31 марта 2026 г

* **Запрет рекурсивных связей и рефакторинг типов сущностей:** Реализована валидация, запрещающая создание рекурсивных связей (объект с самим собой) в модальном окне добавления связи. Исходный объект теперь исключается из списка доступных целей. Также проведен рефакторинг кода для использования `EntityType` вместо строковых литералов для обозначения типов сущностей, что повышает типобезопасность и читаемость.

## 15 марта 2026 г

* **Исправлена регрессия в иерархии при Drag-and-Drop:** В дереве повествования устранена ошибка, позволявшая нарушать иерархию (например, вкладывать сцену напрямую в проект). Логика перемещения (`narrativeReorderer.ts`) была скорректирована так, чтобы при любой попытке вложения запускался механизм интеллектуального поиска корректного родительского элемента.

---

## 5 марта 2026 г

* **Проведено ручное тестирование:** Провели ручное тестирование по `testing_checklist.md`.
* **Аудит технического долга и актуализация документации:** Проведен комплексный аудит функционала, по результатам которого был значительно пополнен список технических задач в `tech_issues.md`. Выявлены и задокументированы несколько критических проблем и багов, включая:
* **Актуализация тестового чеклиста:** В `testing_checklist.md` внесены правки для приведения его в соответствие с текущим функционалом:
  * Удалены или исправлены неактуальные тесты.
  * Добавлена новая проверка для функции частичного экспорта из контекстного меню.

---

## 3 марта 2026 г

* **Реализация Drag-and-Drop для повествования:** Реализована функция для реорганизации элементов в дереве повествования с помощью перетаскивания (drag-and-drop). Ключевой особенностью является механизм "интеллектуальной коррекции" некорректных перемещений, названный "Контекстное Погружение" (Contextual Dive). Если пользователь пытается переместить элемент в невалидную позицию (например, дочерний элемент с меньшим "весом" в родительский с большим), система не просто отменяет действие, а ищет наиболее подходящее валидное место. Алгоритм поднимается вверх по иерархии от места сброса, находит первого подходящего "предка-контейнера", а затем спускается по цепочке его "первенцев", чтобы найти самое глубокое допустимое место для вставки. Это решает как проблемы невалидных вложений, так и причуды UI-библиотеки, которая может неверно сообщать тип сброса ('after' вместо 'inside').

---

## 2 марта 2026 г

* **Реализовано рекурсивное удаление элементов повествования:** Модифицирован `NarrativeService` для удаления элемента вместе со всеми его дочерними элементами и связанными файлами. В `NarrativeDao` добавлены методы для рекурсивного поиска дочерних узлов (`findAllDescendantIds`) и массового удаления (`deleteByIds`). Обновлён диалог подтверждения в UI.
* **Корректное удаление файлов:** Исправлена проблема, из-за которой при удалении объекта мира (`WorldObject`) его папка и файлы оставались на диске. Метод `WorldObjectService.deleteObject` был сделан асинхронным, и теперь он корректно ожидает (`await`) завершения `fileSystemService.deleteDirectory`, обрабатывая возможные ошибки и уведомляя пользователя.

## 1 марта 2026 г

* **Вспомогательные файлы:** Реализована функция для работы со вспомогательными файлами объектов мира.
  * Исправлена кнопка "Открыть папку" для корректной работы через `FileSystemService`.
  * В `FileSystemService` добавлен метод `getDirectoryFiles` для получения списка файлов из директории с фильтрацией (`content.md` и файлы, имена которых не начинаются с буквы или цифры, включая кириллицу).
  * Создан новый компонент `AttachedFiles` для отображения списка файлов в виде сворачиваемой панели с указанием их количества.
  * Основной компонент `ContentDisplay` был отрефакторен для правильного размещения и взаимодействия с новым компонентом `AttachedFiles` и существующими `ConnectionsManager` и `FileContent`.

## 27 февраля 2026 г

* **Автосохранение:** Реализована функция автосохранения изменений при переключении между элементами повествования или объектами мира. Это предотвращает потерю данных, автоматически сохраняя незавершенную работу пользователя без явного запроса.
* **Массовое добавление пунктов плана:** Реализована функция массового добавления пунктов в план повествования. В модальном окне добавления теперь используется многострочное текстовое поле. Вставленный текст парсится, и каждая непустая строка создается как отдельный элемент плана в виде checklist-а. Это позволяет быстро переносить списки из внешних источников.

## 20 февраля 2026 г

* **Исправление дублирования уведомлений:** Устранён баг, при котором пользователю показывались дублирующиеся уведомления (одно из main-процесса через IPC, второе из renderer). Renderer теперь является единым источником истины для уведомлений — main-процесс возвращает ошибки через IPC-ответ, а renderer отображает их.

## 15 февраля 2026 г

* **Исправление ширины полей плана:** В компоненте `ChecklistEditor` заменён `Space` на `div` с flex-контейнером для корректного растягивания полей ввода по ширине контента. Стили вынесены в `App.css`.

## 11 Февраля 2026 г

* **Поведение модала настроек проекта без открытого проекта:** Реализовано блокирование открытия модального окна настроек проекта из меню, если в данный момент не открыт ни один проект. Это устраняет некорректное отображение модала и улучшает пользовательский опыт.
* **Рефакторинг системы уведомлений:** Взамен разрозненных IPC-событий создан единый `MainNotificationService` в `main`-процессе. Он унифицирует отправку всех типов уведомлений (`error`, `warning`, `info`, `success`) в `renderer`-процесс через один IPC-канал `show-notification`. Для типобезопасности введен `enum NotificationType`. В рамках этого рефакторинга улучшено поведение кнопки настроек проекта, когда проект не открыт, теперь отображается предупреждающее сообщение через новую систему.
* **Рефакторинг системы обработки ошибок и логирования:** Рефакторинг системы обработки ошибок и логирования путём замены всех прямых вызовов `console.error` и `console.log` на централизованные сервисы уведомлений (`MainNotificationService` для основного процесса и `notificationService` для процесса рендерера). Включает обработку нетипизированных ошибок (`unknown`) и унификацию сообщений.

## 10 Февраля 2026 г

* **Опционально создаем подпапку для нового проекта:** Добавлена опция в мастер создания проекта, позволяющая пользователю указать, создавать ли новый проект в подпапке с его именем. Реализована санитаризация имени проекта для использования в качестве имени папки.
* **Рефакторинг:** Рефакторинг поля `is_visible` в таблице `entity_templates` на булевый тип. Обновление всех мест в коде (TypeScript типы, DAO-слой, UI-компоненты), где используется `is_visible`, для работы с булевым значением вместо числового (0 или 1).
* **Использование перечислений (Enums):** Выполнен рефакторинг, заменяющий "магические строки" на TypeScript Enums для идентификации типов (`EntityType.Narrative`, `EntityType.WorldObject`) в `ProjectService._validateProjectStructure`, что повышает надежность и читаемость кода.

## 05 февраля 2026 г

* **Настройка пути к внешнему редактору:** Реализована возможность указания пользователем пути к внешнему редактору Markdown в настройках проекта. В случае, если путь указан, файл открывается с использованием заданного редактора; в противном случае используется системное приложение по умолчанию. Добавлен диалог выбора файла в UI настроек.

## 02 февраля 2026 г

* **Рефакторинг настроек проекта:** Проведен рефакторинг системы настроек проекта. В модель данных `ProjectSetting` добавлено поле `uiHint`, разделяющее тип данных (`type`) от его визуального представления. Модифицирован UI (`ProjectSettingsModal`) для отображения полей `readonly`, `file-path` и `textarea` с соответствующими стилями. В схему БД и DAO внесены изменения для поддержки нового поля. При создании проекта теперь автоматически сохраняется версия приложения в `readonly` поле.

* **Синхронизация выбора в деревьях:** Исправлена ошибка, из-за которой выбор элемента в дереве "Объекты мира" не снимал выделение с элемента в дереве "Повествование". Рефакторинг `NarrativeTree` был произведен для управления его состоянием выбора через глобальное состояние в `App.tsx`, что обеспечило консистентное поведение выбора по всему приложению.
